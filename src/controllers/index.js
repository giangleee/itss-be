const requestController = require('./request');
const authController = require('./auth');
const staffController = require('./staff');
const rattingController = require('./ratting');

module.exports = {
  requestController,
  authController,
  staffController,
  rattingController
};
