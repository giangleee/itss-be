const UserService = require('./user-service')
const ratingService = require('./ratting-service')
const requestService = require('./request-service')

module.exports = {
    UserService,
    ratingService,
    requestService,
}
